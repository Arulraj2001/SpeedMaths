import PowersPage from "../learn/powers/page";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Powers Table Exponents Sheet - SpeedMaths",
  description: "Reference exponents power tables for indices (2, 3, 5, 10, 11, 12) with custom printable formats.",
  alternates: {
    canonical: "https://speedmaths.com/powers"
  }
};

export default PowersPage;
