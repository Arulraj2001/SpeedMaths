import TablesPage from "../learn/tables/page";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Multiplication Times Tables (1-50) - SpeedMaths",
  description: "Browse times tables from 1 to 50 with interactive layouts, text-to-speech voice read, bookmarks, and print modes.",
  alternates: {
    canonical: "https://speedmaths.com/tables"
  }
};

export default TablesPage;
